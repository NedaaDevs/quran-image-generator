pre-commit:
  parallel: true
  jobs:
    - name: biome
      run: bunx biome check --write --staged --no-errors-on-unmatched {staged_files}
      glob: "*.{ts,json}"
      stage_fixed: true
    - name: typecheck
      run: bunx tsc --noEmit
      glob: "*.ts"
